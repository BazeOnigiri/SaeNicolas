<UserControl x:Class="Nicolas.UCs.UCModifierCommande"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Infos commande -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Commande n°" FontWeight="Bold"/>
            <TextBlock Text="{Binding NumCommande}" Margin="5,0,20,0"/>
            <TextBlock Text="Date : " FontWeight="Bold"/>
            <TextBlock Text="{Binding DateCommande, StringFormat='{}{0:dd/MM/yyyy}'}" Margin="5,0,20,0"/>
            <TextBlock Text="Prix total : " FontWeight="Bold"/>
            <TextBlock Text="{Binding PrixTotal, StringFormat='{}{0:F2} €'}" Margin="5,0,20,0"/>
        </StackPanel>

        <!-- Détails demandes associées -->
        <TextBlock Text="Demandes associées à la commande :" FontWeight="Bold" Grid.Row="1" Margin="0,0,0,5"/>
        <DataGrid Grid.Row="2" x:Name="dgDemandesAssociees" 
                  ItemsSource="{Binding DemandesAssociees}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  Margin="0,0,0,10"
                  Height="120">
            <DataGrid.Columns>
                <DataGridTextColumn Header="NumDemande" Binding="{Binding NumDemande}" Width="*"/>
                <DataGridTextColumn Header="NomVin" Binding="{Binding NomVin}" Width="*"/>
                <DataGridTextColumn Header="Quantité" Binding="{Binding Quantite}" Width="*"/>
                <DataGridTextColumn Header="Prix du vin" Binding="{Binding PrixVin}" Width="*"/>
                <DataGridTextColumn Header="Date de la demande" Binding="{Binding DateDemande}" Width="*"/>
                <DataGridTemplateColumn Header="Retirer">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Retirer" Command="{Binding DataContext.CmdRetirerDemande, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Toutes les demandes -->
        <TextBlock Text="Toutes les demandes :" FontWeight="Bold" Grid.Row="3" Margin="0,0,0,5"/>
        <DataGrid Grid.Row="3" x:Name="dgToutesDemandes"
                  ItemsSource="{Binding ToutesDemandes}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0,25,0,10"
                  Height="120">
            <DataGrid.Columns>
                <DataGridTextColumn Header="NumDemande" Binding="{Binding NumDemande}" Width="*"/>
                <DataGridTextColumn Header="NomVin" Binding="{Binding NomVin}" Width="*"/>
                <DataGridTextColumn Header="Quantité" Binding="{Binding QuantiteDemande}" Width="*"/>
                <DataGridTextColumn Header="DateDemande" Binding="{Binding DateDemande, StringFormat='{}{0:dd/MM/yyyy}'}" Width="*"/>
                <DataGridTemplateColumn Header="Ajouter">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ajouter" Command="{Binding DataContext.CmdAjouterDemande, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Boutons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Valider" Width="100" Margin="5" Command="{Binding CmdValider}"/>
            <Button Content="Supprimer la commande" Width="150" Margin="5" Command="{Binding CmdSupprimer}"/>
            <Button Content="Retour" Width="100" Margin="5" Command="{Binding CmdRetour}"/>
        </StackPanel>
    </Grid>
</UserControl>